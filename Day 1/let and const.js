// Write your code here. Read input using 'readLine()' and print output using 'console.log()'.
    const PI = Math.PI;
    
    var r = Number(readLine());

    var area = PI * (r * r);

    console.log(area);

    var perimeter = PI * 2 * r;
    console.log(perimeter);